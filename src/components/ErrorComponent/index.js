import React from 'react';

import './styles.css';

const ErrorComponent = () => (
  <div className="errorMessage">
    <h1>Ops! No movies were found, try again.</h1>
  </div>
);

export default ErrorComponent;
